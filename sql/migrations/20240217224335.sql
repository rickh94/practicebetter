-- Disable the enforcement of foreign-keys constraints
PRAGMA foreign_keys = off;
-- Drop "user_scales" table
DROP TABLE `user_scales`;
-- Enable back the enforcement of foreign-keys constraints
PRAGMA foreign_keys = on;
